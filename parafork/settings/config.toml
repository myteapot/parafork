[base]
branch = "main"

[workdir]
root = ".parafork"
rule = "{YYMMDD}-{HEX4}"

[custom]
autoplan = false # when true, init creates and check requires paradoc/Plan.md
autoformat = true

[control]
squash = true
