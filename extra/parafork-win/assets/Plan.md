# 计划（Plan）

> 仅当 `settings/config.toml` 的 `custom.autoplan=true` 时，`init.ps1` 才会创建本文件且 `check.ps1` 会检查其结构。
> 注意：`## Milestones` 和 `## Tasks` 两个标题必须保留为固定字符串（供 `check.ps1` 机械判定）。

## Milestones
- [ ] M1: PARAFORK_TBD

## Tasks
- [ ] T1: PARAFORK_TBD（milestone: M1）— 验收：PARAFORK_TBD
